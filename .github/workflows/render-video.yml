name: Render Knowledge Video (Legacy Disabled on V25)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy direct-AI workflow disabled
    runs-on: ubuntu-latest
    steps:
      - name: Migration notice
        shell: bash
        run: |
          echo "This legacy workflow is intentionally disabled on v25-ai."
          echo "Direct provider credentials are not permitted in the V25 pipeline."
          echo "Use V25 Email OTP Gateway Guard after the authorization gateway is deployed."
          exit 1
